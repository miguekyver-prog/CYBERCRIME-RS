# NPM Configuration for Optimized Performance

# Reduce npm output verbosity
loglevel=warn

# Prevent excessive npm processes
legacy-peer-deps=true

# Use only specified registry
registry=https://registry.npmjs.org

# Increase timeout for slower connections
fetch-timeout=60000

# Disable optional dependencies to reduce overhead
optional=false
